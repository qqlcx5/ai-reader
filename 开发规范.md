# 项目开发规范

本规范适用于本项目的所有功能开发、缺陷修复、重构和代码评审。目标是保持代码简单、可维护、可测试，并让功能代码与基础设施职责清晰分离。

## 1. 基本原则

### 1.1 先理解，再修改

- 修改前先阅读完整调用链：入口、状态、服务、持久化和 UI 展示。
- 优先搜索并复用已有组件、工具函数、类型和业务流程，禁止重复实现已有能力。
- 先判断需求是否真的需要新增代码；能删除就不新增，能复用就不抽象。
- 修复根因，不在每个调用方重复添加补丁。修改共享函数前必须搜索全部调用方。
- 只实现当前明确需求，不为假设中的未来需求增加接口、配置、工厂或抽象层。
- 允许保留最小实现，但不得牺牲输入校验、安全、无障碍、数据完整性和错误处理。

### 1.2 变更边界

- 保持单一职责，尽量少改文件，避免无关重构。
- 不新增依赖，除非现有能力无法满足且收益明确。
- 不得覆盖或回滚用户已有修改。
- 复杂逻辑必须留下一个可运行的测试或最小自检；纯粹的一行转发不强制新增测试。
- 有意接受的性能或复杂度上限使用 `ponytail:` 注释说明限制和升级条件。

## 2. 分层与职责

项目代码按以下职责组织：

- `components/`：页面和交互组合。负责展示、用户操作和调用 store，不直接承担数据库细节。
- `components/ui/`：可复用 UI 基础组件。负责样式、可访问性、焦点、键盘交互、Portal 和第三方 UI 原语适配。
- `components/common/`：跨业务复用的通用业务组件。
- `stores/`：跨组件状态、业务动作和流程编排。不得把纯 UI 状态扩散到全局 store。
- `services/`：外部 API、AI、同步和复杂领域服务。负责协议适配、超时、取消和错误转换。
- `db/repositories/`：IndexedDB/Dexie 数据访问。组件和 store 不直接操作数据库表。
- `types/`：跨层共享类型。类型应表达真实业务约束，避免大量可选字段掩盖非法状态。
- `utils/`：无副作用或边界清晰的通用函数。业务流程不要塞进工具函数。
- `data/`：静态配置和参考数据，不保存运行时状态。

依赖方向应保持为：页面组件 → store/服务 → repository/外部资源。底层模块不得反向依赖页面组件。

## 3. UI 组件规范

### 3.1 组件优先级

1. 优先使用已有的 `components/ui` 或 `components/common` 封装。
2. 没有合适封装时，先判断是否应补充一个可复用封装。
3. 第三方 UI 原语只能在封装组件内部使用。
4. 最后才允许在业务组件中使用原生交互元素，并在变更说明中说明原因。

业务组件中不得直接使用原生或第三方实现的交互控件，包括但不限于：

- `<button>`、`<input>`、`<textarea>`、`<select>`
- dialog、alert dialog、dropdown、menu、popover、select、tooltip
- 任何需要 Portal、定位、焦点管理、键盘导航或无障碍处理的交互

内容展示用的 `div`、`span`、`p`、`ul` 等不属于上述限制。

### 3.2 封装组件设计

- 封装组件必须隐藏第三方库细节；业务层不得导入 `reka-ui` 原语。
- 默认采用数据驱动：通过类型化的 `items`、`options` 或配置对象渲染标准内容。
- 通过语义化事件向外通信，例如 `select`、`confirm`、`cancel`，禁止暴露底层库事件作为业务 API。
- 提供插槽作为兜底：至少考虑 `trigger`、`item`、`prefix`、`suffix` 等定制场景；插槽不能破坏键盘和无障碍行为。
- 组件负责 Portal、定位、层级、焦点管理、键盘操作、关闭策略、ARIA 属性和基础样式。
- 业务组件负责数据、权限、业务动作和错误提示，不负责复制弹层实现。
- Props、事件和插槽必须有明确 TypeScript 类型；避免 `any` 和宽泛的 `Record<string, unknown>`。
- 样式默认保持项目视觉语言；状态使用统一的 hover、focus、disabled、danger 语义。
- 组件只有一个明确职责。若出现大量业务判断，说明边界应下移或拆分。

### 3.3 交互要求

- 可点击区域应足够大，避免用极小悬浮图标承载危险操作。
- 危险操作使用统一的危险色和明确文案；是否确认由风险决定，低风险操作不得无意义增加确认层级。
- 菜单、弹窗和浮层必须脱离文档流，避免被父级 `overflow`、滚动区域或层叠上下文裁切。
- 所有交互控件必须支持键盘操作、可见焦点和可识别的标题或文字。
- loading、disabled、empty、error 和成功反馈必须有明确状态。

## 4. Vue 与 TypeScript

- 使用 `<script setup lang="ts">`，Props、Emits 和公开方法全部类型化。
- 优先使用 `computed` 表达派生状态，避免在模板中堆叠复杂表达式。
- 事件处理函数保持短小；复杂流程下沉到 store 或服务。
- 不在模板中直接写多步业务逻辑、异步流程或副作用。
- 避免 `watch` 维护本可由 `computed` 表达的状态；确需监听时说明触发原因和生命周期。
- 避免隐式共享可变对象。复制消息、会话等实体时使用明确的深拷贝或克隆逻辑。
- 不使用 `any` 绕过类型检查；第三方类型确有缺陷时在边界处做最小类型适配。
- 日期统一使用项目日期工具和 `dayjs`；不得在生产业务代码中散落日期解析、格式化和相对时间算法。
- API、数据库和用户输入均视为不可信边界，必须校验、处理异常并避免泄露敏感信息。

## 5. Store、服务与数据

- Store 对外暴露稳定的业务动作，不暴露底层数据库实现。
- 异步动作必须明确 loading、成功、失败和取消行为，不能静默吞掉异常。
- 切换会话、文档或上下文前，先处理当前状态的持久化和并发任务，避免数据丢失。
- Repository 负责数据库读写和数据形状适配；不得把 UI 状态写入持久化实体，除非该状态属于业务数据。
- 新增或修改持久化字段时同步更新类型、schema、迁移逻辑和相关测试。
- 外部服务错误应在服务边界转换为应用可理解的错误；UI 只展示用户可操作的提示。
- API key、同步配置等敏感数据不得写入日志、错误提示、导出文件的非必要区域或 URL。

## 6. 测试与验证

每次非平凡修改至少完成与变更范围匹配的验证：

- 类型和编译：`pnpm compile`
- 组件行为：测试渲染、事件、键盘/弹层交互和关键状态
- Store/服务：测试成功、失败、取消、边界和持久化结果
- 工具函数：测试正常输入和边界输入
- 修复缺陷时，新增一个能复现原问题的回归测试

测试应验证用户可观察行为，不应过度依赖内部实现、CSS 细节或第三方库内部结构。已有失败若与当前变更无关，需在结果中明确区分，不得为了绿色测试修改无关行为。

## 7. 错误处理与可观测性

- 不忽略 Promise；异步操作必须 `await`、返回或显式处理错误。
- 错误日志包含模块和操作上下文，不包含密钥、完整网页内容或用户隐私。
- 用户可恢复的错误显示简洁、明确、可操作的提示。
- 数据写入失败不得假装成功；必要时保留 UI 状态并提供重试路径。
- 并发和取消场景必须防止旧任务覆盖新状态。

## 8. 代码评审清单

- 是否复用了已有组件、工具、类型和 store 动作？
- 是否把第三方 UI 原语直接泄漏到了业务组件？
- 是否应使用数据驱动封装，并保留插槽兜底？
- 是否存在无请求的抽象、依赖、配置或重构？
- 是否处理 loading、error、empty、cancel、权限和无障碍？
- 是否会改变时区、排序、持久化格式或旧数据兼容性？
- 是否有针对本次行为的最小回归测试？
- 是否通过 `pnpm compile` 和相关测试？

## 9. 提交与变更说明

- 提交保持单一目的，标题清晰描述行为变化。
- 不提交构建产物、临时文件、密钥或本地环境配置。
- 变更说明按“做了什么、为什么、如何验证、已知限制”组织。
- 若使用原生控件、直接使用第三方原语或接受已知复杂度上限，必须在变更说明中注明原因。
