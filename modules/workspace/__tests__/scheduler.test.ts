// Tests moved to lib/workspace/__tests__/ to avoid the WXT auto-discovery
// behavior on `modules/*/index.ts`. The canonical suite lives at
// `lib/workspace/__tests__/scheduler.test.ts`.
export {};
