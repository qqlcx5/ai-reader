/**
 * M4 — Workspace module entry stub.
 *
 * WXT auto-scans modules/*\/index.ts as "user modules" and requires a default
 * export. The real workspace API lives in `@/lib/workspace` (not under
 * `modules/`) to avoid this auto-discovery behavior.
 *
 * Consumers should import from `@/lib/workspace` directly.
 */
export default {};
