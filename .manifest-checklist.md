# Manifest V3 验证清单

> 每次 PR 前对照检查，确保符合 Chrome Web Store 上架要求。
> 评审依据：[chrome-extensions] skill

## 🔴 Critical 必检项

### 1. Manifest 版本
- [ ] `manifest_version: 3`（不是 V2）
- [ ] 没有 V2 API（`chrome.browserAction`, `chrome.tabs.executeScript` 等）
- [ ] `background.service_worker` 而非 `background.scripts`

### 2. Side Panel 打开机制（规则 #2）
- [ ] `manifest.side_panel.default_path` 已配置
- [ ] **`chrome.action.onClicked` 监听器** 或 **`setPanelBehavior({ openPanelOnActionClick: true })`** 已实现
- [ ] **不要**错误使用 `setPanelBehavior({ openPanelOnActionIconClick: true })`
- [ ] 如果使用 `setPanelBehavior`，**不要**同时定义 `default_popup`

### 3. 图标文件（规则 #1）
- [ ] `public/icons/icon-16.png` 存在（16×16px）
- [ ] `public/icons/icon-48.png` 存在（48×48px）
- [ ] `public/icons/icon-128.png` 存在（128×128px）
- [ ] 每个尺寸是**独立文件**（不能复用同一图片）
- [ ] manifest 中引用的所有图标文件 100% 存在

### 4. `chrome.action` 字段（规则 #11）
- [ ] manifest 中存在 `"action": {}`（至少空对象）

### 5. 权限精确声明（规则 #12）
- [ ] **不**使用 `activeTab`（如果需要从 Side Panel 访问 tabs）
- [ ] **使用** `tabs` 权限（如果访问 `tab.url` 或 `tab.title`，规则 #4）
- [ ] **使用** `scripting` 权限（如果动态注入脚本）
- [ ] `host_permissions` 已声明（`<all_urls>` 需要有充分理由）

## 🟠 High 必检项

### 6. Service Worker 状态管理（规则 #7）
- [ ] **没有**在 SW 全局变量中存储状态
- [ ] 所有状态在 `chrome.storage` 中持久化
- [ ] 长期状态用 `chrome.storage.local`
- [ ] 临时状态用 `chrome.storage.session`
- [ ] 避免使用 `setTimeout` / `setInterval`，用 `chrome.alarms` 替代

### 7. Content Script（规则 #6, #13）
- [ ] 大量 DOM 修改使用 `requestAnimationFrame` 批处理
- [ ] **不**使用 `world: 'MAIN'`（除非确实需要）
- [ ] 使用 `addEventListener` 而非内联事件处理器
- [ ] 没有内联脚本

### 8. 异步 onMessage 响应（规则 #5）
- [ ] 异步 `onMessage` 监听器**必须 `return true`**
- [ ] 同步代码用 `await`，不链式 `.then()`
- [ ] 错误处理覆盖所有异步操作

### 9. CSP & 代码执行（规则 #3）
- [ ] 不在扩展页面使用 `eval()` 或 `new Function()`
- [ ] HTML 中没有内联 `<script>`
- [ ] 需要执行第三方代码时使用 `sandbox` / blob URL / srcdoc

## 🟡 Medium 必检项

### 10. Tab 捕获（规则 #16, #17）
- [ ] 使用 `chrome.tabCapture.getMediaStreamId()` 时有 state locking
- [ ] 状态机：idle → starting → recording → stopping → idle
- [ ] 存储在 `chrome.storage.session`
- [ ] 使用 `chrome.desktopCapture.chooseDesktopMedia` 时传入 `targetTab`
- [ ] 已声明 `tabs` 权限

### 11. 通知 & Badge（规则 #11, #15）
- [ ] `chrome.notifications` 的 `iconUrl` 指向真实存在的图片
- [ ] `chrome.action.setIcon` 的图片路径正确
- [ ] 或者使用 data URL（runtime 生成）

### 12. User Scripts（规则 #18）
- [ ] 调用 `chrome.userScripts.*` 前检查 `isUserScriptsAvailable()`
- [ ] 使用 `configureWorld({ messaging: true })` 配置消息
- [ ] 监听 `onUserScriptMessage` 而非 `onMessage`
- [ ] 用户脚本 `id` 不以下划线开头
- [ ] 每个 `ScriptSource` 都有 `code` 或 `file`（不能都没有或都有）

### 13. Windows API（规则 #19）
- [ ] 使用 `getAll` / `getLastFocused` / `getCurrent`
- [ ] **不**使用 `chrome.windows.query()`（不存在此方法）

## 📋 上架前必检

### 14. CHROMEWEBSTORE.md
- [ ] 包含完整的权限理由（每个权限的中文说明）
- [ ] 包含隐私政策 URL
- [ ] 至少 1 张 1280×800 截图
- [ ] 包含版本历史

### 15. 打包检查
- [ ] ZIP 排除 `.git/`, `node_modules/`, `.env`, `CHROMEWEBSTORE.md`
- [ ] manifest 字段完整（`name`, `version`, `description`）
- [ ] 没有 V2 API 残留

### 16. 权限理由（最关键）
每个权限都要有**具体的中文理由**，不能写"扩展需要"：

| 权限 | 理由示例 |
|------|----------|
| `sidePanel` | "用于显示 AI 对话界面" |
| `storage` | "用于保存用户配置、对话历史和捕获的文档" |
| `tabs` | "用于读取当前标签页 URL 和标题" |
| `scripting` | "用于在用户主动点击时注入内容脚本" |
| `host_permissions: <all_urls>` | "用于在用户主动点击时解析任意网页正文" |

---

## 自动化检查脚本（推荐）

```bash
# 在 CI 中运行：
node scripts/check-manifest.mjs
```

该脚本应检查：
- manifest_version = 3
- 所有 icon 文件存在
- 没有 V2 API
- 必填字段完整
- 权限理由在 CHROMEWEBSTORE.md 中

---

**检查签名**：
- [ ] 本次 PR 已通过所有必检项
- [ ] 已更新 CHROMEWEBSTORE.md（如有变更）
- [ ] 已在浏览器中实际测试
