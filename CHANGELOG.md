# Changelog

## 0.3.0 (unreleased)

### 知识库问答（RAG 2.0）
- 聊天输入框「知识库」开关：提问前自动检索相关文档作答，回答带 [n] 引用标签，点击引用直接打开文档
- bigram 二阶段重排序（修复 CJK 分词盲区）；检索范围可选全库或任意合集

### 洞察面板
- 新「洞察」视图：本周剪藏/词数/复习、最长连续打卡、近 14 天剪藏柱状图、活跃标签与主要来源排行

### 每日 AI 简报
- 设置开启后每天定时（可配小时）用默认模型生成晨报文档：昨日剪藏要点 + 复习看板 + 今日建议；支持「立即生成」

### 页面监控
- 新增「监控」视图与 pageWatches 表（DB v15）：对任意 URL 做正文哈希巡检，变更记录历史（含旧版摘录），周期随 RSS 刷新，变化时面板提醒
- 变更自动生成 AI 一句话摘要（默认模型，后台异步）

### 静态站点导出
- 记忆库多选「导出站点」：生成无依赖静态 HTML 站点 ZIP（index 按月分组 + 每篇独立页），可托管任意静态服务

### 命令面板动作化
- Ctrl/⌘+K 面板新增动作：抓取当前页 / 为当前文档生成闪卡 / 翻译当前文档 / 切换亮暗色

### 全局入口
- Omnibox：地址栏 `am <关键词>` 打开侧栏并自动进入命令面板搜索
- 聊天语音输入（Web Speech API，中文优先，实时中间结果提示）

### 语义向量检索
- OpenAI 兼容 /embeddings 端点（设置可配）；向量缓存表（DB v16）按 contentHash 自动重建
- 知识库问答升级为混合检索：关键词 + 语义 RRF 融合排名（CJK 语义盲区补齐）

### RAG 引用体验
- 回答中的 [1][2] 标记变为可点击胶囊上标，点击直达被引文档

### 静态站点搜索页
- 导出站点内置 search.html：预建索引 + 原生 JS 实时搜索（CJK 双字母分词），零依赖

### 系统通知与阅读目标
- 监控变更/简报生成推系统通知，点击直达对应视图（含待办动作机制）
- 记忆库头部每日阅读目标进度环（词数，可点击修改；连续达标 🔥）

### 主题聚类与知识库智能
- 洞察页「自动主题」：语义向量 k-means 聚类 + AI 命名主题，点击文档直达
- RAG 多轮记忆：追问时上轮引用文档保持可检索（会话级，上限 8 篇）
- 文档挂载后快捷提问 chips：总结/关键点/术语/考点
- 复习页听力模式：自动朗读当前卡正反面
- 导出站点自带 atom.xml 订阅源

### 批量导入与组织增强
- 记忆库「链接」按钮：粘贴 URL 列表批量抓取入库（后台 fetch+提取，逐条进度，连续 6 次失败止损）
- 洞察页主题卡片「存为合集」：聚类结果一键固化为合集
- 复习页可按合集缩小队列；卡片库可按来源文档过滤
- 每日简报可选 Webhook 推送（飞书/钉钉 msg_type 格式或通用 JSON）

### 知识库体检与效率工具
- 新「体检」视图：健康分仪表盘 + 五类检测（空壳/未打标/积压未读/近重复/高亮漂移），一键清空壳、逐对删重复，可选死链扫描（HEAD/GET，付费墙 401/403 视为存活）
- 上下文工具栏三连：导出 PDF（打印优化页）、下一篇未读（连续阅读）、一键智能处理（AI 打标签→生成闪卡）

## 0.2.0

### 剪藏
- **PDF / arXiv 剪藏**：面板内 fetch + pdf.js 提取全文（按坐标重排段落、去页码）；arXiv 链接优先抓官方 HTML 版，无 HTML 回退 PDF
- **YouTube 字幕剪藏**：内容脚本提取字幕轨（优先中文），生成带时间戳的 Markdown，无字幕回退普通抓取
- **右键菜单**：「剪藏此页面到 AuraMind」/「保存选中文字到 AuraMind」（选区直接存为引用式快速笔记）
- **键盘快捷键**：`Alt+Shift+C` 抓取当前页、`Alt+Shift+R` 打开复习
- **AI 自动打标签**（可选）：抓取入库后自动生成 3-5 个主题标签
- **音频转写**：本地音频文件（≤25MB）经 Whisper 兼容端点转写入库；RSS 播客条目（`<enclosure>`）一键转写

### 复习（新子系统）
- **SM-2 间隔重复**：AI 从高亮生成**问答卡**或**挖空卡**，四档评分 + 键盘打分（空格/1234）
- **连续打卡 streak**、累计复习数、近 14 天热力图
- **到期角标**：浏览器图标显示待复习数（99+ 封顶）
- **每日新卡上限**（0=不限）、**闪卡挂起/恢复**、**闪卡编辑**
- **每日重温**：确定性挑选 30 天前的旧剪藏
- **Anki 导出**：TSV 文件或 **AnkiConnect 直推**（自动建牌组、重复跳过）
- **闪卡云同步**：并入 WebDAV/S3 数据集，按 updatedAt 三方合并

### 阅读 & 组织
- **[[双向链接]]** 与**知识图谱**（力导向布局，灰圈为未创建文档）
- **相关文档推荐**：字符二元组相似度（中英文通吃）
- **AI 对照翻译**：段落级对齐、分批翻译、代码块不译、缺段回退，译文缓存随同步走
- **TTS 听文档**：Web Speech API，长文分块，中文优先音色
- **阅读器目录 TOC**：≥4 标题时浮动目录按钮，点击平滑定位
- **阅读时长**：记忆库卡片显示「约 N 分钟」
- **命令面板**：`Ctrl/⌘+K` 全局搜文档 + 跳视图；`?` 快捷键速查表

### 获取
- **Newsletter 收件箱**：自托管 Cloudflare Email Worker（`doc/newsletter-worker/` 一键部署）+ 扩展定期拉取
- OPML 导入导出（已有，本轮确认）

### 导出 & 备份
- Obsidian 兼容 Markdown ZIP：YAML frontmatter + `==高亮==` 小节；支持全库 / 多选 / **合集**导出
- **高亮导出**：单篇高亮一键导出 Markdown
- 全库 JSON 备份/恢复补齐 flashcards（旧备份兼容）

### 平台
- **Firefox 构建**（`pnpm build:firefox`，MV2 + sidebar_action，编程式开侧栏有回退）；后台 RSS 刷新双引擎可用（Firefox 在持久后台页内直接解析，Chromium 走 offscreen）
- **实验性暗色模式**（亮/暗/跟随系统，全局 CSS 翻转，程序化验收通过）

### 内部
- 测试从 530 → 613+，全绿；lucide mock 改为 `importOriginal` 透传
- UnoCSS 误扫 `[mm:ss]` 字面量导致构建失败的修复
- 捕获管线统一 `captureTab` 分流（web/pdf/arxiv/youtube）

## 0.1.0

初始版本：剪藏、AI 对话、高亮、RSS、WebDAV/S3 同步、本地优先。
