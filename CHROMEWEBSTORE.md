# Chrome Web Store 上架元数据

> ReadChat Chrome Extension 的 Chrome Web Store 上架信息
> 依据：[chrome-extensions] skill Part 2 - Publishing
> 最后更新：2026-06-25

---

## 1. 基本信息

- **名称**：ReadChat
- **简短描述**（≤ 132 字符）：
  > 在浏览器侧边栏中与任意网页内容对话，基于 AI 的智能阅读助手
- **详细描述**（≤ 16,000 字符）：
  > ReadChat 是一款 Chrome 浏览器扩展，让你在阅读任何网页时都能在侧边栏中与 AI 对话。
  >
  > 核心功能：
  > 1. **一键捕获**：点击悬浮按钮，自动提取网页正文和元数据
  > 2. **沉浸对话**：在侧边栏中基于整篇文档与 AI 对话，支持流式输出
  > 3. **本地存储**：所有捕获的文档和对话历史存储在本地，保护隐私
  > 4. **全文检索**：毫秒级搜索所有历史捕获内容
  > 5. **多模型支持**：兼容所有 OpenAI 格式 API（DeepSeek、通义千问、OpenRouter 等）
  > 6. **备份同步**：支持手动导出和 WebDAV 远程同步
  > 7. **时间轴视图**：以日历热力图形式展示你的阅读足迹
  >
  > 隐私优先：所有数据存储在本地，不上传服务器，API Key 加密保存。

- **类别**：生产力工具（Productivity）
- **语言**：中文（简体）、English

---

## 2. 权限理由（Permissions Justification）

> ⚠️ 这是审核最容易出问题的部分。每个权限都必须有**具体的中文理由**。

| 权限 | 用途 | 中文理由 |
|------|------|----------|
| `sidePanel` | 打开侧边栏 UI | 用于在浏览器右侧打开 AI 对话界面，让用户在阅读网页时可以同时与 AI 对话 |
| `storage` | 本地存储配置和数据 | 用于保存用户的模型配置、API Key、对话历史、捕获的网页内容（全部本地存储，不上传服务器） |
| `tabs` | 读取当前标签页信息 | 用于在用户主动点击悬浮按钮时，获取当前页面的 URL 和标题，让 AI 知道用户正在阅读哪篇文章 |
| `scripting` | 注入内容脚本 | 用于在用户主动点击时向网页注入内容脚本以提取正文 |
| `host_permissions: <all_urls>` | 访问任意网页 | 用于在用户主动点击时，读取任意网页的 DOM 内容以提取正文（仅在用户点击时触发，不自动执行） |

---

## 3. 隐私政策（Privacy Policy）

### 数据收集

**ReadChat 不收集任何用户数据。** 所有信息（捕获的网页、AI 对话历史、API Key、配置）均存储在用户本地浏览器中（Chrome 本地存储 API），不会上传到任何远程服务器。

### 第三方服务

用户在配置第三方 AI 模型（如 DeepSeek、OpenAI 等）时，ReadChat 会将以下信息发送给该 AI 服务商：
- 用户输入的问题
- 用户主动捕获的网页正文（用于 AI 理解上下文）

**这是用户主动触发的行为**，且 ReadChat 不存储或转发这些数据。

### 远程备份（可选）

用户在主动配置 WebDAV 后，可以将本地数据备份到用户自己的 WebDAV 服务器。ReadChat **不**提供 WebDAV 服务器，**不**访问用户配置的 WebDAV 内容，**不**传输数据到任何 ReadChat 控制的服务器。

### API Key 安全

用户的 API Key 存储在 `chrome.storage.local` 中，仅在本机使用，不会同步到 Chrome 账户或任何远程服务器。

### 联系方式

如对隐私政策有疑问，请联系：[GitHub Issues URL]

---

## 4. 视觉资源

### 截图清单

- [ ] `screenshot-1.png` - 主对话界面（1280×800）
- [ ] `screenshot-2.png` - 模型配置界面（1280×800）
- [ ] `screenshot-3.png` - 时间轴视图（1280×800）
- [ ] `screenshot-4.png` - 搜索结果（1280×800）
- [ ] `screenshot-5.png` - 悬浮按钮和捕获流程（1280×800）

### 图标

- [ ] `icon-16.png` (16×16px)
- [ ] `icon-48.png` (48×48px)
- [ ] `icon-128.png` (128×128px)
- [ ] `promo-small.png` (440×280px) - 小型宣传图
- [ ] `promo-large.png` (920×680px) - 大型宣传图（可选）
- [ ] `promo-marquee.png` (1400×560px) - 横幅宣传图（可选）

### 视频

- [ ] `demo.mp4` - 演示视频（≤ 30 秒，可选）

---

## 5. 版本历史

| 版本 | 日期 | 变更摘要 |
|------|------|----------|
| 0.1.0 | 待定 | MVP：基础捕获、对话、模型配置 |
| 0.2.0 | 待定 | 增加：搜索、时间轴 |
| 0.3.0 | 待定 | 增加：WebDAV 同步、导入导出 |
| 1.0.0 | 待定 | 正式版本 |

---

## 6. 商店设置

### 分发

- [ ] 公开（Public）
- [ ] 或：不公开（Unlisted）— 仅通过链接访问

### 可见性

- [ ] 所有国家/地区
- [ ] 或：仅 [指定国家]

### 定价

- 免费（Free）

---

## 7. 提交前检查清单

参考 [chrome-extensions] skill 的 `references/webstore/review-checklist.md`：

- [ ] 所有权限都有具体的中文理由
- [ ] 隐私政策 URL 有效（可托管在 GitHub Pages）
- [ ] 至少 1 张 1280×800 截图
- [ ] ZIP 不含 `.git/`, `node_modules/`, `.env`, `CHROMEWEBSTORE.md`
- [ ] manifest_version = 3
- [ ] 没有 V2 API 残留
- [ ] 在 Chrome 中实际测试通过
- [ ] 商店描述不包含实现细节（API、库名等）
- [ ] 商店描述使用受益导向语言

---

## 8. 审核常见拒绝理由预防

| 拒绝理由 | 预防措施 |
|----------|----------|
| 权限理由不充分 | 每个权限都有具体中文理由（见第 2 节） |
| 单一权限用途 | 明确区分 `sidePanel` / `tabs` / `scripting` 的不同用途 |
| 隐私政策不完整 | 提供完整的隐私政策（见第 3 节） |
| 描述含实现细节 | 用户描述只讲功能，不讲 API/库名 |
| 截图模糊 | 至少 1 张 1280×800 清晰截图 |
| 图标引用错误 | 严格遵守 chrome-extensions 规则 #1 |
| Side Panel 无法打开 | 必须实现打开触发器（规则 #2） |
| 使用 `activeTab` 错误 | 从 Side Panel 触发时用 `tabs` + `host_permissions` |

---

**下一步行动**：
1. 截图资源（需开发完成后）
2. 创建隐私政策页面（GitHub Pages）
3. 完善图标设计
4. 准备首次发布版本
